on:
  workflow_dispatch:
  schedule:
    # - cron: '15 */55 * * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: li1ht2ay/it1h-a2@cron
        with:
          token: ${{ secrets.token }}
